sudo sh -c 'echo "=== sshd effective settings (sshd -T) ==="; sshd -T 2>/dev/null | awk "/clientaliveinterval|clientalivecountmax|logingracetime/ {print}"; echo; echo "=== sshd_config explicit lines ==="; grep -nE "^\s*(ClientAliveInterval|ClientAliveCountMax|LoginGraceTime)\b" /etc/ssh/sshd_config 2>/dev/null || echo "none found"; echo; echo "=== TMOUT in /etc (systemwide shell timeout) ==="; grep -RIn --color=never "^\s*TMOUT\b" /etc/profile /etc/profile.d /etc/bashrc /etc/bash.bashrc /etc/environment 2>/dev/null || echo "none found"'
